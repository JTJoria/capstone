<h1>Joining you on this trip: </h1>

<h1>Name: <%= @person.name %></h1>
<h1>Gender: <%= @person.gender %></h3>
<h1>Age: <%= @person.age %></h3>

<a href="/persons/new">Add Another Trip Mate <span class="btn btn-default"></span></a>
<a href="/situations">That's Everybody <span class="btn btn-default"></span></a>
<hr>
<%= link_to "Home", "/persons" %>
<%= link_to "Edit", "/persons/#{@person.id}/edit" %>
<%= link_to "Delete", "/persons/#{@person.id}", method: :delete, data:{confirm: "Are You Sure?"} %>