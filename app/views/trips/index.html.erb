<h1>Your Trips</h1>

<% if creator_signed_in? %>

  <% @this_trip.each do |trip| %>
    <h4><a href="/trips/<%= trip.id %>"><%= trip.city %></a></h4>
    <h5> State: <%= trip.state %></h5>
    <h5> Start Date: <%= trip.start_date %></h5>
    <h5> End Date: <%= trip.end_date %></h5>
    <br>
  <% end %>

<% else %>

<h3><%= No trips so far! Try adding one below! %></h3>

<% end %>

<h3><a href="/locations">Create a New Trip<span class="btn btn-default"></span></a></h3>

